/** The mark's own blue — the flat fill of the original scan (sampled #053D60,
    mean #063D5F over 43k core pixels). Kept apart from the engine so the page
    shell can colour its × without pulling the renderer into its chunk. */
export const LOGO_BLUE = '#053D60';
